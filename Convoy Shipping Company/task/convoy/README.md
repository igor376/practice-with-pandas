Here I practice with pandas framework. 
Now it imports from xlsx to csv(does correction data by removing everything except digits),
after that imports csv to sqlite database, and imports from s3db to json.
